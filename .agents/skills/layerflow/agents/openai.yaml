interface:
  display_name: "Layerflow"
  short_description: "Repo-specific patterns and workflows for layerflow"
  default_prompt: "Use the layerflow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true